#include "load_balancer_server.h"
#include "config.h"
#include "load_balancer_server.h"

int main() {
    load_balancer_server server;
    server.start_server();
    config conf;
    load_balancer_server server;

    server.start_server();

    return 0;
}
